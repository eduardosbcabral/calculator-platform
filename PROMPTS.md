# AI Assistance

AI tools helped plan, implement, test, review, and deploy this repository. The prompts below were translated from Portuguese and lightly edited for clarity.

## Architecture and planning

> Use `$loop-orchestration`, `$saas-backend-patterns`, `$saas-frontend-patterns`, and `$ponytail` to review the requirements and plan a simple React and Go solution. Apply only principles that fit idiomatic Go and React. Prefer the Go standard library and return the proposed architecture, assumptions, testing strategy, and implementation plan before writing code.

## Implementation

> Proceed with the approved plan using `$loop-orchestration`, `$ponytail`, and `$rtk-token-saver`. Implement the React and TypeScript frontend and the Go backend. Keep all code and documentation in English, avoid unnecessary abstractions and dependencies, and include strict validation, an accessible responsive interface, tests, coverage, a benchmark, CI, a production container, and deployment configuration. Run and verify every check before delivery.

## Modularity review

> Review and refactor the implementation using `$loop-orchestration`, `$saas-backend-patterns`, `$saas-frontend-patterns`, and `$ponytail`. Find components, methods, or handlers with multiple responsibilities and improve their organization without adding framework-style layers or speculative abstractions. Add focused regression tests for changed behavior.

## Final review

> Use `$review-agent` to review the backend, frontend, infrastructure, and documentation. Run read-only checks first, identify bugs, reliability or security risks, stale claims, and unnecessary complexity, then rank findings by severity with file and line evidence. Do not change files until the findings are approved.

## Deployment and verification

> Use `$coolify-cli`, `$propeller-vps`, and `$agent-browser` to deploy and validate the completed application. Preserve unrelated infrastructure and verify the deployed commit, health check, HTTPS, API behavior, responsive interface, and CI result.

All generated work was reviewed, executed, and validated against the repository checks.
